import {loadConfig,dbPath} from './config/env.js';import {openDatabase} from './db/client.js';import {EnrollmentRepository} from './db/repositories/EnrollmentRepository.js';import {SampleRepository} from './db/repositories/SampleRepository.js';import {registerCommands} from './discord/registerCommands.js';import {createDiscordClient} from './discord/client.js';import {TempFiles} from './audio/tempFiles.js';import {logger} from './utils/logger.js';import {FishClient} from './services/fish/FishClient.js';import {DiscordVoiceRuntime} from './voice/DiscordVoiceRuntime.js';import {VoiceCaptureService} from './voice/VoiceCaptureService.js';import {TrainingSessionManager} from './training/TrainingSessionManager.js';import {VoiceModelBuilder} from './training/VoiceModelBuilder.js';import {FishTranscriptionService} from './services/transcription/FishTranscriptionService.js';import {DiscordLiveService} from './live/DiscordLiveService.js';import {ProviderDeletionQueue} from './services/fish/ProviderDeletionQueue.js';import {DatasetCurator} from './training/DatasetCurator.js';
async function main(){const config=loadConfig();const temp=new TempFiles();await temp.init();const db=openDatabase(dbPath(config.DATABASE_URL));const repo=new EnrollmentRepository(db);const samples=new SampleRepository(db);const fish=new FishClient(config.FISH_API_KEY,'https://api.fish.audio',config.FISH_REALTIME_TIMEOUT_MS,config.FISH_TTS_MODEL,config.FISH_REALTIME_MODEL);const providerDeletions=new ProviderDeletionQueue(db,fish);providerDeletions.start();const voice=new DiscordVoiceRuntime();const capture=new VoiceCaptureService('/data/training',undefined,undefined,config.MAX_TRAINING_SEGMENT_SECONDS*1000,bytes=>samples.reserveStorage(bytes,config.MAX_TRAINING_STORAGE_MB*1024*1024),config.MIN_TRAINING_SEGMENT_SECONDS*1000);const training=new TrainingSessionManager();const builder=new VoiceModelBuilder(fish,id=>providerDeletions.delete(id),new DatasetCurator(config.MODEL_SAMPLE_MIN_SCORE),config.FISH_MAX_MODEL_REFERENCES,config.MAX_SELECTED_TRAINING_DURATION_SECONDS);const transcription=new FishTranscriptionService(config.FISH_API_KEY);const live=new DiscordLiveService(transcription,fish,voice,config.MAX_LIVE_LAG_MS,config.MAX_LIVE_UTTERANCE_MS);await registerCommands(config.DISCORD_TOKEN,config.DISCORD_CLIENT_ID,config.DISCORD_GUILD_ID);const client=createDiscordClient(config,repo,{samples,fish,voice,capture,training,builder,live,providerDeletions});let shuttingDown=false;for(const signal of ['SIGINT','SIGTERM'] as const)process.once(signal,()=>void (async()=>{if(shuttingDown)return;shuttingDown=true;logger.info({signal},'graceful shutdown started');client.removeAllListeners('interactionCreate');await capture.stopAllAndDrain();await live.stopAll();await voice.stopAll();await providerDeletions.stop();db.close();client.destroy();logger.info({signal},'graceful shutdown complete');process.exit(0);})());await client.login(config.DISCORD_TOKEN);logger.info('bot started');}
main().catch(e=>{logger.fatal({err:e},'startup failed');process.exitCode=1;});
