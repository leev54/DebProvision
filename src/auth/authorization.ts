export class RateLimiter {private hits=new Map<string,number[]>();private calls=0;constructor(private limit=5,private windowMs=10_000){}allow(id:string,now=Date.now()){if(++this.calls%100===0)for(const [key,hits] of this.hits)if(!hits.some(x=>now-x<this.windowMs))this.hits.delete(key);const active=(this.hits.get(id)??[]).filter(x=>now-x<this.windowMs);if(active.length>=this.limit){this.hits.set(id,active);return false;}active.push(now);this.hits.set(id,active);return true;}size(){return this.hits.size;}}
