export interface QueueItem {id:string;label:string;play:()=>Promise<void>;cleanup?:()=>Promise<void>}
export class PlaybackQueue {private items:QueueItem[]=[];private active?:QueueItem;private running=false; enqueue(item:QueueItem){this.items.push(item);void this.drain();} list(){return [this.active,...this.items].filter(Boolean) as QueueItem[];} skip(){this.active=undefined;} async stop(){this.items=[];this.active=undefined;}private async drain(){if(this.running)return;this.running=true;while((this.active=this.items.shift())){const x=this.active;try{await x.play();}finally{await x.cleanup?.();if(this.active===x)this.active=undefined;}}this.running=false;}}
